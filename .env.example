# =====================================================
# DATABASE CONFIGURATION
# =====================================================
# Local development database URL
# Format: postgresql://<username>:<password>@<host>:<port>/<database>?schema=<schema>
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/crowdfund?schema=public"

# =====================================================
# NEXT.JS CONFIGURATION
# =====================================================
# Base URL for API requests (must end with trailing slash)
# Used for client-side API calls
NEXT_PUBLIC_API_URL="http://localhost:3000/"

# Docker environment API URL (used when running in Docker)
# NEXT_PUBLIC_API_URL="http://app:3000/"
